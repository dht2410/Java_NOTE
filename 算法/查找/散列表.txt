散列表
解决散列冲突的方法：拉链法和线性探测法

散列函数
将整数散列的常用方法是除留余数法，余数最好取素数
将浮点数散列方法是将键表示为二进制数然后再使用除留余数法
字符串：Horner方法

如果a.equals(b)返回值是true，则a.hashcode()==b.hashcode().
但如果两个对象的hashcode相同，不一定equal
如果要为自定义的数据类型定义散列函数，则需要同时重写hashcode()和equals()方法
